#!/bin/bash
set -euxo pipefail


# copy new version of code
cat docker-compose.prod.yaml | ssh -i $DEPLOY_KEY_PATH $DEPLOY_USER@$DEPLOY_INSTANCE """
cat - > /tmp/docker-compose.prod.yaml
docker-compose -f /tmp/docker-compose.prod.yaml pull
docker-compose -f /tmp/docker-compose.prod.yaml down
docker-compose -f /tmp/docker-compose.prod.yaml up -d
"""
